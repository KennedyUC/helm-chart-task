apiVersion: v1
kind: Service
metadata:
  labels:
    service: {{ .Values.backend.name }}
  name: {{ .Values.backend.name }}
spec:
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
  selector:
    service: {{ .Values.backend.name }}